{
  "name": "estilobazar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "build:hom": "vite build --mode staging",
    "build:prd": "vite build --mode production",
    "deploy:hom": "npx firebase-tools hosting:channel:deploy hom",
    "deploy:prd": "npx firebase-tools deploy --only hosting",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  },
  "dependencies": {
    "firebase": "^11.3.1"
  }
}
